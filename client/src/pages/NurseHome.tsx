import React from 'react';
import NurseDashboard from './NurseDashboard';

/**
 * NurseHome Component
 * 
 * This component serves as the landing page for nurses when they log in.
 * It renders the NurseDashboard to provide quick access to key nurse functions.
 */
const NurseHome: React.FC = () => {
  return <NurseDashboard />;
};

export default NurseHome; 